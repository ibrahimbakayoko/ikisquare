import PropTypes from 'prop-types';
//import { graphql } from "gatsby";
import Layout from "../components/layout"
import React, { Component } from 'react'
/*
const Blog = ({ data}) => {
     //const articles = data.allStrapiArticles.edges.node;
     console.log("SO \n\n here \n is \n  the new \n")
     console.log(data.allStrapiArticles.edges[0].node.id)
    return (
        <Layout>
          <div className="article-page-body">
                {data.allStrapiArticles.edges[0].node.id}
                {data.allStrapiArticles.edges[0].node.articleContent}
                <div>hello from blog date</div>
          </div>
        </Layout>
    ) 
};

 Blog.propTypes = {
    data : PropTypes.object.isRequired,
} 
export const query = graphql`
  query( $id: String! ) {
    allStrapiArticles(filter: {id: {eq: $id}}) {
      edges {
        node {
          id
          articleContent
        }
      }
    }
  }
` 
  export default Blog ;*/
  